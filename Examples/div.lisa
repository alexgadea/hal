

vardef x: Int;
vardef y: Int;
vardef q: Int;
vardef r: Int
{ Pre: x = X ∧ y = Y ∧ ¬ (Y = 0) ∧ ¬ (Y < 0) ∧ ¬ (X < 0)}

q := 0;
r := x;
while (y < r || y = r) { x = y * q + r ∧ ¬ (r < 0)}
    do
        r := r - y;
        q := q + 1;
    od;

{ Post: X = Y * q + r ∧ ¬ (r <= 0) ∧ r < Y }


